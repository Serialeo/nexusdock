name: Publish container

on:
  workflow_dispatch:
  push:
    tags:
      - 'v*'

permissions:
  contents: read

jobs:
  publish:
    name: Publish container images
    runs-on: ubuntu-latest
    timeout-minutes: 30
    permissions:
      contents: read
      packages: write
    steps:
      - uses: actions/checkout@v4

      - uses: docker/setup-buildx-action@v3

      - name: Login to GitHub Container Registry
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Prepare image metadata
        id: meta
        uses: docker/metadata-action@v5
        with:
          images: ghcr.io/serialeo/nexusdock
          tags: |
            type=sha,prefix=sha-,format=short
            type=raw,value=${{ github.ref_name }},enable=${{ startsWith(github.ref, 'refs/tags/v') }}
            type=semver,pattern={{version}}
            type=semver,pattern={{major}}.{{minor}}
            type=raw,value=latest,enable=${{ startsWith(github.ref, 'refs/tags/v') }}

      - name: Build and push image
        id: push
        uses: docker/build-push-action@v6
        with:
          context: .
          file: Dockerfile
          platforms: linux/amd64
          push: true
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
          cache-from: type=gha,scope=nexusdock-runtime
          cache-to: type=gha,mode=max,scope=nexusdock-runtime,ignore-error=true

      - name: Report published digest
        run: echo "digest=${{ steps.push.outputs.digest }}"

  verify:
    name: Verify public container pulls
    needs: publish
    runs-on: ubuntu-latest
    timeout-minutes: 10
    permissions:
      contents: read
    steps:
      - uses: docker/setup-buildx-action@v3

      - name: Ensure GHCR verification is anonymous
        run: docker logout ghcr.io >/dev/null 2>&1 || true

      - name: Resolve verification tag
        id: tag
        shell: bash
        run: |
          if [[ "$GITHUB_REF" == refs/tags/v* ]]; then
            echo "value=${GITHUB_REF_NAME}" >> "$GITHUB_OUTPUT"
          else
            echo "value=sha-${GITHUB_SHA::7}" >> "$GITHUB_OUTPUT"
          fi

      - name: Pull and run published images without credentials
        shell: bash
        env:
          VERIFY_TAG: ${{ steps.tag.outputs.value }}
        run: |
          set -euo pipefail
          images=("ghcr.io/serialeo/nexusdock:${VERIFY_TAG}")
          for image in "${images[@]}"; do
            echo "Verifying ${image}"
            docker pull "$image"
            name="nexus-verify-${RANDOM}"
            docker run -d --name "$name" -e NEXUS_AUTH_TOKEN=release-verification "$image"
            ready=false
            for _ in $(seq 1 30); do
              status="$(docker inspect -f '{{if .State.Health}}{{.State.Health.Status}}{{else}}none{{end}}' "$name")"
              if [[ "$status" == healthy ]]; then
                ready=true
                break
              fi
              if [[ "$(docker inspect -f '{{.State.Status}}' "$name")" == exited ]]; then
                docker logs "$name"
                exit 1
              fi
              sleep 2
            done
            if [[ "$ready" != true ]]; then
              docker inspect "$name"
              docker logs "$name"
              exit 1
            fi
            docker inspect -f '{{json .RepoDigests}}' "$image"
            docker rm -f "$name"
          done
